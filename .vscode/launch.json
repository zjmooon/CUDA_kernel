{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CUDA Debug (With OpenCV)",
            "type": "cuda-gdb",       // 改为 CUDA 专用调试器
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "preLaunchTask": "CUDA and OpenCV Build" 
        }
    ]
}
// 设置条件断点只观察第一个线程 threadIdx.x == 0 && blockIdx.x == 0 && blockIdx.y == 0(一维block， 二维grid)